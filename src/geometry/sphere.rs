pub struct Sphere {
    pub radius: f64;
}

pub struct SphereIntersection {
    intersections: [2]f64;
}

impl Sphere {
    pub fn intersect(sphere: Sphere) -> Option<SphereIntersection> {
        return None;
    }
}
